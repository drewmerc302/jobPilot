#set page(margin: 1in)
#set text(font: "Helvetica", size: 11pt)

= jobPilot — Typst bundling spike

If you can read this PDF, then Typst was successfully:

+ Bundled inside the Briefcase `.app` resources directory
+ Located at runtime by `_typst_binary()`
+ Invoked via `subprocess.run` from the bundled Python interpreter

Compiled at #datetime.today().display().
